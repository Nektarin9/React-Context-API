import { createContext } from "react";


export const AppContextSearch = createContext(null)
export const AppContextSorting = createContext(null)
export const AppContextOnTodoAdd = createContext(null)
